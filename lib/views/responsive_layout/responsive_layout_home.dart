part of '../../helpers/lulz_imports.dart';

class ResponsiveLayoutHome extends StatelessWidget {
  const ResponsiveLayoutHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
