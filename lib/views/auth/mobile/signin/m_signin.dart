import 'package:flutter/material.dart';

class MSignIn extends StatelessWidget {
  const MSignIn({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Mobile Sign in'))
      
    );
  }
}