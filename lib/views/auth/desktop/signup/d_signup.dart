import 'package:flutter/material.dart';

class DSignUp extends StatelessWidget {
  const DSignUp({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child:  Text('DSignUp'))
      
    );
  }
}